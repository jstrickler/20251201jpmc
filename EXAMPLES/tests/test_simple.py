def test_two_plus_two_equals_four():  # tests should begin with "test" (or will not be found automatically)
    assert 2 + 2 == 4   # if assert statement succeeds, the test passes

def test_empty_str_len_is_zero():
    print("Hello JPMC")
    assert len("") == 0
